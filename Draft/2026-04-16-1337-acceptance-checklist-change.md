# Acceptance Checklist Change

## 结论
已新增系统预期功能验收清单，并在 `AGENTS.md` 中规定后续功能完善必须同步维护该清单。本次达到预期。

## 改动原因
- 用户要求把系统预期功能整理成可验收文档。
- 该文档用于把 `/Draft/毕设.md` 的大方向转化为后续可执行、可追踪的功能清单。

## 具体改动
- 新增 `Draft/2026-04-16-1337-acceptance-checklist.md`。
- 修改 `AGENTS.md`，新增“功能验收清单维护规则”。
- 更新 `Draft/CHANGE-INDEX.md` 记录入口。
- 未修改业务代码、接口、配置或数据库结构。

## 改动原因说明
- 使用独立验收清单，避免后续开发者只凭聊天上下文或旧进度文档判断完成度。
- 保留带时间戳文件名，符合现有文档命名规则，同时在 `AGENTS.md` 中固定该文件作为后续维护对象。
- 未做更大范围重构，因为本次任务目标是文档和流程约束。

## 风险与注意事项
- 当前验收清单基于现有代码与 Draft 文档整理，后续需求变化时必须持续更新。
- 如果后续功能只是 demo 或模拟，不得把状态标记为已完成。

## 验证情况
- 已读取新增验收清单和索引内容。
- 未运行构建，因为本次没有修改业务代码。

## 下一位 agent 的接手提示
- 后续功能开发前先看 `Draft/2026-04-16-1337-acceptance-checklist.md`。
- 开发完成后必须更新该清单的状态和维护记录。
