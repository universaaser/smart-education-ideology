## 1. 全局工作原则

### 1.1 回复与沟通规则
- 始终使用中文回复。
- 回答结构必须遵循：**先给结论，再给步骤**。
- 不说空话，不做无意义复述。
- 若发现用户要求存在冲突、不合理或会导致错误，必须直接指出原因，并给出更合理的处理方式。
- 不得省略关键限制条件、风险说明和未完成项。

### 1.2 修改代码的基本原则
- 优先采用**最小改动**方案，避免大范围重构。
- **不改无关代码**，仅修改与当前任务直接相关的部分。
- 必须**保持现有代码风格**，包括但不限于：
  - 命名风格
  - 缩进风格
  - 文件组织方式
  - 现有框架用法
  - 已有工具链和依赖习惯
- 除非任务明确要求，否则不要擅自：
  - 更换框架
  - 替换库
  - 调整目录结构
  - 引入新依赖
  - 修改公共接口
- 修改代码时，必须说明**改动原因**，且原因应与当前任务直接相关。

### 1.3 命令与环境约束
- 给出的命令默认适配 **Windows PowerShell**。
- 若某命令依赖特定环境（如 Node、Python、Java、Docker、pnpm、maven 等），必须明确说明前提。
- 若命令存在高风险（删除、覆盖、重置、迁移等），必须明确标注风险。

---

## 2. 项目目标与任务优先级

### 2.1 最终目标
- 系统的最终目标是实现 `/Draft/毕设.md` 文件内定义的全部功能。

### 2.2 规则补充来源
- 若 `Draft` 文件夹中存在新的约束、说明、补充规则、接口文档、设计说明，则这些内容应视为当前项目规范的一部分。
- 当 `/Draft/毕设.md` 与其他 Draft 文档存在冲突时，优先级如下：

1. 用户当前明确提出的直接要求
2. `/Draft/毕设.md`
3. `Draft` 文件夹中的其他补充规范
4. 本文件 `AGENTS.md`
5. 一般性默认工程习惯

- 如果发现规则之间存在冲突，必须在执行时显式说明冲突点，并选择影响最小、最符合最终目标的方案。

### 2.3 每次任务的对齐要求
每次开始任务前，应先判断当前任务与 `/Draft/毕设.md` 的关系，属于以下哪一种：
- 直接实现目标功能
- 修复实现目标功能过程中的缺陷
- 为实现目标功能补充基础设施或辅助能力
- 与最终目标无关

如果任务与最终目标无关，应优先提醒并说明偏离点。

---

## 3. 代码实现规范

### 3.1 代码语言与注释规则
- 在代码中加入**详细注释**，尤其是：
  - 核心业务逻辑
  - 边界条件处理
  - 容易误解的流程
  - 兼容性处理
  - 临时折中方案
- **代码中除了注释不要写中文**。
- 所有标识符、变量名、函数名、类名、接口名、文件名、日志文本、错误消息、配置键名等，默认使用英文。
- 注释可以使用中文，且应写清楚“为什么这样改”“这段逻辑解决什么问题”。

### 3.2 修改策略
- 先复用现有实现，再考虑新增实现。
- 能在原位置修复，就不要额外包一层。
- 能局部修复，就不要整体重写。
- 若必须新增文件，文件职责必须单一且必要。
- 若必须修改接口、数据结构、配置或数据库，必须说明：
  - 改动原因
  - 影响范围
  - 是否兼容旧逻辑
  - 是否需要迁移或额外验证

### 3.3 风险控制
- 不得为了“看起来更完整”而伪造未验证的功能。
- 不得在未说明的情况下引入潜在破坏性行为。
- 对未验证部分必须明确写出：
  - 已完成什么
  - 未验证什么
  - 可能风险是什么

---

## 4. 文档输出与变更记录规范

### 4.1 文档输出位置
- 所有新增的任务文档、变更说明、阶段总结、设计补充、交接说明，默认保存在 `Draft` 文件夹中。

### 4.2 文件命名要求
- 输出文档时，**必须在文档名称中加入当前时间，便于区分**。
- 文件名格式统一为：

`Draft/YYYY-MM-DD-HHMM-<short-name>.md`

示例：
- `Draft/2026-04-16-1430-change-log.md`
- `Draft/2026-04-16-1515-feature-progress.md`
- `Draft/2026-04-16-1600-task-handoff.md`

- `<short-name>` 必须使用英文短词或短横线连接，避免中文文件名导致兼容性问题。

### 4.3 什么时候必须输出文档
出现以下情况时，必须输出一份文档到 `Draft` 文件夹：
- 修改了业务逻辑
- 新增、删除、重命名文件
- 修改接口、配置、数据结构、数据库结构
- 修复了非显而易见的 bug
- 做了重构
- 进行了分阶段任务推进
- 任务尚未完全结束，但需要为下一个全新上下文窗口提供接手依据

以下情况可以不单独写文档：
- 纯格式化
- 纯注释调整
- 纯拼写修复
- 明显无语义变化的小改动

若不确定是否需要文档，则默认**要写**。

### 4.4 文档目标
文档必须同时满足两个目的：
1. 方便人工审查本次改动
2. 方便一个**全新上下文窗口的 agent** 快速理解当前状态并继续工作

因此文档必须：
- 简洁
- 去重
- 可快速扫描
- 避免长篇空话
- 不粘贴大段 diff
- 优先写清路径、模块、原因、影响、下一步

### 4.5 单篇文档长度限制
- 建议控制在 **150-300 字/词级别的信息密度**对应的短文档范围内
- 一般不要超过 **500 字/词级别的有效内容**
- 复杂架构性变更可适当放宽，但仍应避免冗长
- 不要堆砌代码
- 不要重复仓库里已经显而易见的信息

### 4.6 文档模板
每次输出文档时，默认使用以下模板：

```md
# <Title>

## 结论
用几句话说明本次改动完成了什么，是否达到预期。

## 改动原因
- 说明为什么需要这次修改
- 说明它和 `/Draft/毕设.md` 的对应关系

## 具体改动
- 修改了哪些文件
- 每个文件改了什么
- 是否新增/删除文件
- 是否影响接口、配置、数据结构

## 改动原因说明
- 为什么采用当前方案
- 为什么没有采用更大范围重构
- 为什么这是最小改动方案

## 风险与注意事项
- 未完成项
- 潜在风险
- 兼容性问题
- 依赖前提

## 验证情况
- 已执行的检查、测试、构建、手动验证
- 未验证项必须明确写出

## 下一位 agent 的接手提示
- 下一步最应该看什么
- 相关文件路径
- 当前卡点或待办
```

## 5. 面向全新上下文窗口的续接规则

### 5.1 续接优化目标

所有输出都必须考虑：后续接手的 agent 可能没有当前聊天上下文，只能看到仓库文件和 Draft 文档。

因此，文档必须优先写：

- 改动目标
- 关键文件路径
- 核心决策
- 未完成事项
- 风险点
- 下一步建议

不要写大量过程叙述，不要写无关背景故事。

### 5.2 推荐维护索引文件

建议维护一个总索引文件：

```
Draft/CHANGE-INDEX.md
```

用途：

- 按时间倒序记录重要文档
- 让新 agent 先看索引再定位详细文档

推荐格式：

```
# Change Index

- `2026-04-16 15:30` | `order-cache-fix` | fixed stale cache after status update | files: `src/service/order.ts`, `src/cache/order-cache.ts`
- `2026-04-16 14:10` | `login-flow-update` | aligned auth flow with draft requirements | files: `src/api/auth.ts`, `src/store/user.ts`
```

### 5.3 当前状态文件

如果任务链较长，建议额外维护：

```
Draft/CURRENT-STATE.md
```

用于记录：

- 当前主要目标
- 最新决定
- 未完成问题
- 下一步最重要的触点文件

该文件必须很短，避免冗长。

------

## 6. 回答与交付格式规范

### 6.1 默认回答结构

默认按以下结构输出：

1. 结论
2. 步骤
3. 改动原因
4. 影响范围
5. 验证方式
6. 风险或未完成项

### 6.2 修改代码时必须说明

每次修改代码时，必须明确说明：

- 改了什么
- 为什么改
- 是否是最小改动
- 是否影响无关模块
- 是否保持现有风格

### 6.3 给出代码时的要求

- 代码应可直接落地
- 注释详细
- 注释可用中文
- 除注释外不出现中文
- 不输出与当前任务无关的大段样板代码

------

## 7. 任务完成判定

只有在同时满足以下条件时，任务才算完成：

- 已完成与当前任务直接相关的代码修改
- 已说明改动原因
- 已尽量保持最小改动且未改无关代码
- 已保持现有代码风格
- 已补充必要注释
- 已根据需要输出文档到 `Draft` 文件夹
- 文档文件名已带当前时间
- 已说明验证情况
- 已说明未完成项和风险（如存在）

若代码已改但未输出应有的文档，则任务**不算完成**。

------

## 8. 禁止事项

- 不要为了“看起来先进”而主动大改架构。
- 不要无依据地新增依赖。
- 不要擅自修改无关模块。
- 不要在代码中加入中文标识符、中文日志、中文字符串（注释除外）。
- 不要输出冗长、重复、低信息密度的文档。
- 不要把大段 diff 原样贴进文档。
- 不要忽略 `/Draft/毕设.md` 和 `Draft` 文件夹中的补充规则。

---

## 9. 功能验收清单维护规则

### 9.1 固定验收清单
- 后续进行任何功能完善、业务逻辑修改、接口补充、页面新增、配置调整、数据库结构调整时，必须同步维护以下验收清单：

`Draft/2026-04-16-1337-acceptance-checklist.md`

- 该文件是系统预期功能的主验收依据，用于说明：
  - 系统最终应该具备哪些功能
  - 当前哪些功能已经完成
  - 哪些功能只是 demo 或临时妥协
  - 哪些功能仍未完成
  - 下一位 agent 应该如何继续推进

### 9.2 每次功能完善后的必做事项
- 必须在验收清单中更新对应条目的状态：
  - `[x]` 表示已达到可验收状态
  - `[~]` 表示已有 demo、框架或部分实现，但仍未达到正式验收
  - `[ ]` 表示尚未实现或缺少闭环
- 必须在验收清单的“维护记录”中追加一条记录，写明：
  - 本次改动时间
  - 本次改动名称
  - 已改动的部分
  - 涉及的关键文件
  - 仍需要继续完成的部分
  - 未验证项和风险
- 如果新增功能不在当前验收清单中，必须先补充新的验收项，再进行或总结实现。

### 9.3 与其他文档的关系
- `/Draft/毕设.md` 仍是最终目标来源。
- `Draft/2026-04-16-1337-acceptance-checklist.md` 是把 `/Draft/毕设.md` 转化为可执行、可验收、可续接任务的工作清单。
- 若用户当前明确要求、`/Draft/毕设.md`、验收清单、其他 Draft 文档之间存在冲突，优先级仍按本文件第 2.2 节执行。
- 发生冲突时，必须在验收清单“维护记录”中说明冲突点、取舍原因和后续影响。

### 9.4 完成判定补充
- 后续凡是进行功能完善，如果没有同步更新 `Draft/2026-04-16-1337-acceptance-checklist.md`，即使代码已经实现，也不算任务完成。
- 如果功能只是临时 demo、模拟数据、兜底逻辑或未接真实服务，不能标记为 `[x]`，只能标记为 `[~]`，并写明正式项目仍需要补什么。
