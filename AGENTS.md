# AGENTS.md

## 0. 最高优先级：四项核心执行原则

以下四项原则是本项目中所有实现、修改、说明、验证与交付行为的最高约束。若与一般工程习惯冲突，以这四项原则为准；若与用户当前直接要求冲突，以用户当前直接要求为准。

### 0.1 Think Before Coding

先思考，再动手；不允许在关键点上默认脑补。

- 不得在需求存在歧义时静默选择一种解释并直接实现。
- 若存在多种合理解释，必须先明确列出：
  - 当前理解
  - 其他可能解释
  - 各自影响
  - 拟采用方案及原因
- 若不确定信息会影响实现结果，优先澄清，而不是猜测。
- 若发现用户要求、现有代码、文档约束、接口行为之间存在冲突，必须显式指出，不得装作没看到。
- 若存在更简单、更小风险的方案，必须主动提出，不得默认选择更复杂实现。
- 若对局部代码、注释、配置或历史行为理解不足，不得擅自改写；先说明不确定点，再决定是否继续。

### 0.2 Simplicity First

始终选择能解决问题的最小实现，禁止为了“更通用”“更优雅”“更可扩展”而预支复杂度。

- 不做用户未要求的功能扩展。
- 不为一次性逻辑引入额外抽象。
- 不新增未经要求的可配置项、开关、层级、模式或包装器。
- 不为不可能或未出现的场景堆砌错误处理。
- 若 50 行能解决，就不要写成 200 行。
- 若现有实现只需局部修补，就不要整体重写。
- 写完后必须反问：这份实现是否会被一个有经验的工程师评价为“过度设计”？如果会，继续简化。

### 0.3 Surgical Changes

只改必须改的内容；只清理自己造成的问题；不顺手“优化”无关部分。

- 每一处改动都必须能直接追溯到当前任务。
- 不得借任务之机顺手重构相邻模块、格式化无关文件、改写无关注释、调整无关命名。
- 不得删除、修改自己并未充分理解的现有代码、注释、配置或兼容逻辑。
- 发现无关死代码、历史问题、风格问题时，可以指出，但不要擅自处理。
- 只移除“因本次改动而变成无用”的 import、变量、函数或分支。
- 不得删除既有死代码，除非用户明确要求。
- 必须保持现有代码风格；即使你不喜欢，也不要在无关处改风格。

### 0.4 Goal-Driven Execution

把任务转化为可验证目标，而不是模糊执行。

- 实现前必须明确本次任务的成功标准。
- 对 bug 修复类任务：优先先构造复现方式，再修复并验证。
- 对校验、规则、接口类任务：优先定义输入、输出、边界和失败条件，再实现。
- 对重构类任务：必须说明“哪些行为不能变”，并验证变更前后结果一致。
- 多步骤任务默认按以下格式组织：
  1. 步骤
  2. 验证方式
  3. 完成判据
- 不允许用“应该可以”“理论上没问题”代替验证。

### 0.5 简单任务的例外

对于明显的一行修改、纯文案修正、纯注释修正、低风险拼写修复，可适度简化分析与文档流程；但仍必须遵守：

- 不猜测
- 不过度设计
- 不改无关代码
- 结果可验证

---

## 1. 全局工作原则

### 1.1 回复与沟通规则

- 始终使用中文回复。
- 默认回答结构：**先给结论，再给步骤**。
- 不说空话，不做无意义复述。
- 若发现用户要求不合理、互相冲突、实现路径错误或成本明显失衡，必须直接指出原因，并给出更合理方案。
- 不得隐瞒关键限制条件、风险说明、兼容性影响、未完成项与未验证项。
- 对非平凡任务，开始实现前必须先给出：
  - 当前理解
  - 成功标准
  - 拟执行步骤
  - 主要风险或歧义
- 若存在多种实现路径，优先给出简明 tradeoff，而不是静默拍板。

### 1.2 修改代码的基本原则

- 优先采用**最小改动**方案，避免大范围重构。
- **不改无关代码**，仅修改与当前任务直接相关的部分。
- 必须**保持现有代码风格**，包括但不限于：
  - 命名风格
  - 缩进风格
  - 文件组织方式
  - 现有框架用法
  - 已有工具链和依赖习惯
- 除非任务明确要求，否则不要擅自：
  - 更换框架
  - 替换库
  - 调整目录结构
  - 引入新依赖
  - 修改公共接口
- 修改代码时，必须说明：
  - 改了什么
  - 为什么改
  - 为什么这是最小改动方案
  - 是否影响无关模块
  - 是否保持现有风格

### 1.3 命令与环境约束

- 给出的命令默认适配 **Windows PowerShell**。
- 若某命令依赖特定环境（如 Node、Python、Java、Docker、pnpm、Maven 等），必须明确说明前提。
- 若命令存在高风险（删除、覆盖、重置、迁移等），必须显式标注风险。

---

## 2. 项目目标与任务优先级

### 2.1 最终目标

- 系统的最终目标是实现 `/Draft/毕设.md` 文件内定义的全部功能。

### 2.2 规则补充来源

- 若 `Draft` 文件夹中存在新的约束、说明、补充规则、接口文档、设计说明，则这些内容视为当前项目规范的一部分。
- 当规则存在冲突时，优先级如下：

1. 用户当前明确提出的直接要求
2. `/Draft/毕设.md`
3. `Draft` 文件夹中的其他补充规范
4. `Draft/2026-04-16-1337-acceptance-checklist.md`
5. 本文件 `AGENTS.md`
6. 一般性默认工程习惯

- 如果发现规则冲突，必须显式说明：
  - 冲突点
  - 采用方案
  - 取舍原因
  - 影响范围

### 2.3 每次任务开始前的对齐要求

每次开始任务前，必须先判断当前任务与 `/Draft/毕设.md` 的关系，属于以下哪一种：

- 直接实现目标功能
- 修复实现目标功能过程中的缺陷
- 为实现目标功能补充基础设施或辅助能力
- 与最终目标无关

如果任务与最终目标无关，应优先提醒并说明偏离点。

### 2.4 任务启动时的最小分析要求

对于非平凡任务，在真正修改代码前，必须先明确：

- 目标是什么
- 成功标准是什么
- 现有代码中最可能需要修改的位置是什么
- 是否存在歧义、依赖前提或冲突约束
- 是否需要测试、构建、脚本或手动验证

若这些内容尚不清楚，不得直接进入实现。

---

## 3. 代码实现规范

### 3.1 代码语言与注释规则

- 在代码中加入**必要且有价值的详细注释**，尤其是：
  - 核心业务逻辑
  - 边界条件处理
  - 容易误解的流程
  - 兼容性处理
  - 临时折中方案
- **代码中除了注释不要写中文**。
- 所有标识符、变量名、函数名、类名、接口名、文件名、日志文本、错误消息、配置键名等，默认使用英文。
- 注释可以使用中文，但必须说明“为什么这样改”或“这段逻辑解决什么问题”，避免写成低价值废话注释。
- 若发现现有注释、字符串或文档中出现疑似乱码字符，不得因为“看起来像乱码”就整段删除；这可能只是中文内容与当前打开编码不一致。应先判断是否为编码问题，再决定是否修改。
- 不得无故删除或改写原有注释；只有当其与本次改动直接冲突、已明显失真或已被本次改动破坏时，才可同步修正。

### 3.2 修改策略

- 先复用现有实现，再考虑新增实现。
- 能在原位置修复，就不要额外包一层。
- 能局部修复，就不要整体重写。
- 不为潜在未来需求预埋抽象。
- 若必须新增文件，文件职责必须单一且必要。
- 若必须修改接口、数据结构、配置或数据库，必须说明：
  - 改动原因
  - 影响范围
  - 是否兼容旧逻辑
  - 是否需要迁移或额外验证

### 3.3 风险控制

- 不得为了“看起来完整”而伪造未验证功能。
- 不得在未说明的情况下引入潜在破坏性行为。
- 对未验证部分必须明确写出：
  - 已完成什么
  - 未验证什么
  - 可能风险是什么
- 若发现更简单方案足以满足目标，应优先回退到更简单方案。

### 3.4 与无关代码隔离

- 不得顺手修复无关 lint、无关 warning、无关格式问题，除非它们直接阻碍本次任务完成。
- 不得重排无关 import、换行、排序、注释格式。
- 若本次改动导致新 orphan（无用 import、变量、函数），应当清理。
- 若无关 orphan 在改动范围内被观察到，可以备注，但不要未经要求擅自删除。

---

## 4. 验证与成功标准

### 4.1 默认验证原则

- 每次任务都必须定义本次任务的成功判据。
- 成功判据必须可检查、可描述、可复核。
- 禁止以“逻辑上应该没问题”代替验证结果。

### 4.2 不同任务的默认验证方式

#### Bug 修复

- 先说明 bug 的复现条件。
- 若项目具备测试条件，优先补充或编写复现测试。
- 修复后验证复现路径不再触发问题。

#### 新功能

- 明确输入、输出、边界条件、失败路径。
- 若项目具备测试条件，优先补充测试。
- 至少说明手动验证路径与预期结果。

#### 重构

- 先说明哪些外部行为必须保持不变。
- 验证重构前后结果一致。

#### 配置/脚本/构建调整

- 明确前提条件。
- 明确执行命令。
- 明确成功输出或可观察结果。

### 4.3 若无法测试

若受限于环境、依赖、权限或上下文，无法完整测试，必须明确写出：

- 为什么无法测试
- 已做了哪些替代性检查
- 仍然存在哪些不确定性

---

## 5. 文档输出与变更记录规范

### 5.1 文档输出位置

- 所有新增的任务文档、变更说明、阶段总结、设计补充、交接说明，默认保存在 `Draft` 文件夹中。

### 5.2 文件命名要求

- 输出文档时，**必须在文档名称中加入当前时间，便于区分**。
- 文件名格式统一为：

`Draft/YYYY-MM-DD-HHMM-<short-name>.md`

示例：

- `Draft/2026-04-16-1430-change-log.md`
- `Draft/2026-04-16-1515-feature-progress.md`
- `Draft/2026-04-16-1600-task-handoff.md`

- `<short-name>` 必须使用英文短词或短横线连接，避免中文文件名导致兼容性问题。

### 5.3 什么时候必须输出文档

出现以下情况时，必须输出一份文档到 `Draft` 文件夹：

- 修改了业务逻辑
- 新增、删除、重命名文件
- 修改接口、配置、数据结构、数据库结构
- 修复了非显而易见的 bug
- 做了重构
- 进行了分阶段任务推进
- 任务尚未完全结束，但需要为下一个全新上下文窗口提供接手依据

以下情况可以不单独写文档：

- 纯格式化
- 纯注释调整
- 纯拼写修复
- 明显无语义变化的小改动

若不确定是否需要文档，则默认**要写**。

### 5.4 文档目标

文档必须同时满足两个目的：

1. 方便人工审查本次改动
2. 方便一个**全新上下文窗口的 agent** 快速理解当前状态并继续工作

因此文档必须：

- 简洁
- 去重
- 可快速扫描
- 避免长篇空话
- 不粘贴大段 diff
- 优先写清路径、模块、原因、影响、下一步

### 5.5 单篇文档长度限制

- 建议控制在 **150-300 字/词级别的信息密度**对应的短文档范围内。
- 一般不要超过 **500 字/词级别的有效内容**。
- 复杂架构性变更可适当放宽，但仍应避免冗长。
- 不要堆砌代码。
- 不要重复仓库里已经显而易见的信息。

### 5.6 文档模板

每次输出文档时，默认使用以下模板：

```md
# <Title>

## 结论

用几句话说明本次改动完成了什么，是否达到预期。

## 改动原因

- 说明为什么需要这次修改
- 说明它和 `/Draft/毕设.md` 的对应关系

## 具体改动

- 修改了哪些文件
- 每个文件改了什么
- 是否新增/删除文件
- 是否影响接口、配置、数据结构

## 方案取舍

- 列出采用当前方案的原因
- 说明为什么没有采用更复杂或更大范围重构的方案
- 若存在歧义，说明最终如何定夺

## 风险与注意事项

- 未完成项
- 潜在风险
- 兼容性问题
- 依赖前提
- 未验证项

## 验证情况

- 已执行的检查、测试、构建、手动验证
- 验证结果
- 未验证项必须明确写出

## 下一位 agent 的接手提示

- 下一步最应该看什么
- 相关文件路径
- 当前卡点或待办
```

---

## 6. 面向全新上下文窗口的续接规则

### 6.1 续接优化目标

所有输出都必须考虑：后续接手的 agent 可能没有当前聊天上下文，只能看到仓库文件和 Draft 文档。

因此，文档必须优先写：

- 改动目标
- 关键文件路径
- 核心决策
- 未完成事项
- 风险点
- 下一步建议

不要写大量过程叙述，不要写无关背景故事。

### 6.2 推荐维护索引文件

建议维护一个总索引文件：

`Draft/CHANGE-INDEX.md`

用途：

- 按时间倒序记录重要文档
- 让新 agent 先看索引再定位详细文档

推荐格式：

```md
# Change Index

- `2026-04-16 15:30` | `order-cache-fix` | fixed stale cache after status update | files: `src/service/order.ts`, `src/cache/order-cache.ts`
- `2026-04-16 14:10` | `login-flow-update` | aligned auth flow with draft requirements | files: `src/api/auth.ts`, `src/store/user.ts`
```

### 6.3 当前状态文件

如果任务链较长，建议额外维护：

`Draft/CURRENT-STATE.md`

用于记录：

- 当前主要目标
- 最新决定
- 未完成问题
- 下一步最重要的触点文件

该文件必须很短，避免冗长。

---

## 7. 回答与交付格式规范

### 7.1 默认回答结构

默认按以下结构输出：

1. 结论
2. 步骤
3. 改动原因
4. 影响范围
5. 验证方式或验证结果
6. 风险、限制或未完成项

### 7.2 修改代码时必须说明

每次修改代码时，必须明确说明：

- 改了什么
- 为什么改
- 是否是最小改动
- 是否影响无关模块
- 是否保持现有风格
- 如何验证

### 7.3 给出代码时的要求

- 代码应可直接落地。
- 注释应服务于理解，而不是凑数量。
- 注释可使用中文。
- 除明确保留的历史内容外，代码中的新增标识符、日志、错误消息和字符串默认不出现中文；注释按上文规则处理。
- 不输出与当前任务无关的大段样板代码。
- 若实现存在取舍，应说明为什么没有选择更复杂方案。

---

## 8. 任务完成判定

只有在同时满足以下条件时，任务才算完成：

- 已完成与当前任务直接相关的代码修改。
- 已说明改动原因。
- 已尽量保持最小改动且未改无关代码。
- 已保持现有代码风格。
- 已补充必要注释。
- 已根据需要输出文档到 `Draft` 文件夹。
- 文档文件名已带当前时间。
- 已说明验证情况。
- 已说明未完成项和风险（如存在）。
- 已同步维护验收清单（若本次任务涉及功能实现、功能状态变化或验收状态变化）。

若代码已改但未输出应有文档，或未同步更新必须维护的验收清单，则任务**不算完成**。

---

## 9. 禁止事项

- 不要在需求不清时靠猜测直接实现。
- 不要隐瞒冲突、歧义、未知点和取舍。
- 不要为了“看起来先进”而主动大改架构。
- 不要无依据地新增依赖。
- 不要擅自修改无关模块。
- 不要顺手重构、顺手格式化、顺手清理无关代码。
- 不要删除或改写自己并未充分理解的代码和注释。
- 不要把疑似乱码内容直接当作无意义文本批量删除；先确认是否只是编码显示问题，尤其是历史中文注释、文案和文档。
- 不要在代码中加入中文标识符、中文日志、中文字符串（注释除外）。
- 不要输出冗长、重复、低信息密度的文档。
- 不要把大段 diff 原样贴进文档。
- 不要忽略 `/Draft/毕设.md`、验收清单和 `Draft` 文件夹中的补充规则。

---

## 10. 功能验收清单维护规则

### 10.1 固定验收清单

- 后续进行任何功能完善、业务逻辑修改、接口补充、页面新增、配置调整、数据库结构调整时，必须同步维护以下验收清单：

`Draft/2026-04-16-1337-acceptance-checklist.md`

- 该文件是系统预期功能的主验收依据，用于说明：
  - 系统最终应该具备哪些功能
  - 当前哪些功能已经完成
  - 哪些功能只是 demo 或临时妥协
  - 哪些功能仍未完成
  - 下一位 agent 应如何继续推进

### 10.2 每次功能完善后的必做事项

- 必须在验收清单中更新对应条目的状态：
  - `[x]` 表示已达到可验收状态
  - `[~]` 表示已有 demo、框架或部分实现，但仍未达到正式验收
  - `[ ]` 表示尚未实现或缺少闭环
- 必须在验收清单的“维护记录”中追加一条记录，写明：
  - 本次改动时间
  - 本次改动名称
  - 已改动的部分
  - 涉及的关键文件
  - 仍需要继续完成的部分
  - 未验证项和风险
- 如果新增功能不在当前验收清单中，必须先补充新的验收项，再进行或总结实现。

### 10.3 与其他文档的关系

- `/Draft/毕设.md` 仍是最终目标来源。
- `Draft/2026-04-16-1337-acceptance-checklist.md` 是把 `/Draft/毕设.md` 转化为可执行、可验收、可续接任务的工作清单。
- 若用户当前明确要求、`/Draft/毕设.md`、验收清单、其他 Draft 文档之间存在冲突，优先级仍按本文件第 2.2 节执行。
- 发生冲突时，必须在验收清单“维护记录”中说明冲突点、取舍原因和后续影响。

### 10.4 完成判定补充

- 后续凡是进行功能完善，如果没有同步更新 `Draft/2026-04-16-1337-acceptance-checklist.md`，即使代码已经实现，也不算任务完成。
- 如果功能只是临时 demo、模拟数据、兜底逻辑或未接真实服务，不能标记为 `[x]`，只能标记为 `[~]`，并写明正式项目仍需补什么。
